// A scalar value that scales the capturing rectangle
// If compared to image viewing tools, 1 and 1 means no scaling
// 0.5 will zoom in to a 200% view, 3 will zoom out and produce 33% scaling
ct.camera.scale.x = 1.2;
ct.camera.scale.y = 1.2;
ct.camera.scale = 1.2;